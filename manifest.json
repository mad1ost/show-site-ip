{
  "manifest_version": 2,
  "name": "Show server IP",
  "version": "0.3.0",
  "description": "Show server IP when hovering over the icon in the address bar and copy it to the clipboard when clicking",
  "background": {
    "scripts": ["background.js"]
  },
  "page_action": {
    "browser_style": true,
    "default_icon": "icons/ip.svg",
    "default_title": "Show server IP",
    "show_matches": ["*://*/*"]
  },
  "permissions": [
    "*://*/*",
    "webRequest",
    "clipboardWrite"
  ]
}
